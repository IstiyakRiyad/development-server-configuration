# Sentry Deployment


## For initial setup
Please make sure your `SENTRY_SECRET_KEY` key is empty in `.env` file.

```bash
sudo ./install.sh
```

## Stop sentry
```bash
sudo docker compose down
```

## Restart the service
```bash
sudo ./install.sh
```
